<!-- 文件用途：定义规划页交互、日期规则、持久化接口和验证边界。 -->

# 任务规划

规划取代旧项目投入月历，内部导航仍为 `calendar`。首页负责当日执行；规划的日期是独立页面状态，默认本地今天，浏览不会更改首页日期。

## 页面行为

- 手机与桌面默认进入月历首页，点击任意日期进入独立的当天详情视图，详情内保留周一开头的周条。用“返回月历”回到来源月份，焦点回到原日期格；跨月补位日期同样适用。页面切换由组件状态管理，不创建独立 URL 或浏览器历史记录。
- 桌面采用完整月历或单日时间轴。月历底部待安排入口进入今天详情并展开、定位待安排池；返回后恢复该入口焦点。
- 月历仅统计 active 普通任务，包含完成项，排除待安排、放弃、回收站和 Daily。零项日期只显示日期；非零日期显示小号“x 项”，数量不截断。四档非零热力为 1–2、3–4、5–7、8+，只表示安排密度，不代表任务时长。
- 用户选择雾蓝配色：浅色热力从 `#edf3fa` 到 `#a7c5e5`，深色从 `#232f40` 到 `#3b638e`。日期默认 17px，数量 0.6875rem（默认 11px）、常规字重；小字用独立颜色保持对比。今天用实心圆标记，所选日期用细描边；周条只用小圆点提示有安排，完整数量由可访问名称提供。
- 手机月格高度随可用屏幕高度收紧，保留完整可点击格与小字间距；窄屏、横屏和放大文字允许页面自然滚动。
- 当天详情采用紧凑页头、滚动时吸顶的周条和小时画布。未定时间与待安排收进时间轴上方同一行的折叠入口，展开后占满内容宽度；已完成收进下方折叠区，默认收起。预计只累计未完成任务，缺少估时单独标记。
- 时间块按填写的同日起止时间排位，不使用独立预计投入推算结束时间。没有结束时间的任务用中性背景起点标记并显示“结束未定”。默认画布为 08:00–20:00，早晚任务自动扩展至覆盖全部安排；今天显示由真实时钟驱动的现在指示，其他日期隐藏。
- 每小时固定为 6rem；短于 30 分钟的任务预留可点击空间，左侧细色条仍按真实时长绘制。任务碰撞按显示空间分栏，避免短任务相邻时互相遮挡；超过三列的连续重叠组显示统一入口，点开可查看组内所有任务。最晚任务可在 24:00 刻度后保留点击空间，这不代表创建了次日安排。
- 点开时间块进入安排详情，保留完整标题、项目、起止时间与独立预计，继续复用完成、编辑、改期和退回待安排动作。操作后只更新原任务，不复制日程记录；关闭弹窗恢复来源控件焦点。
- 新建固定使用打开表单时的浏览日期；过去日期可查看和编辑已有任务，但不提供新增、安排目标。任务可编辑、完成、改期或退回待安排。
- 待安排保留重要/普通分组，显式按钮显示目标日期；提交中防重复，失败保留原数据并显示错误。编辑、改期使用共用可访问模态组件；月历与详情之间不再使用弹窗。

## 设计参考

参考 [Apple 日历月视图](https://support.apple.com/guide/iphone/change-how-you-view-events-iphfd1054569/26/ios/26) 的月/日信息层级，以及 [Fantastical 的月份浏览](https://flexibits.com/fantastical-ios/help/calendar-views) 对日期定位和密度的区分。最终视觉按用户确认保留数量与同色相热力，使用雾蓝方案，并把数量降低为次要文字；没有照搬产品界面或引入其他配色开关。

## 持久化与兼容

普通字段使用新增 `saveTaskConfirmed(task)` 命令，首页与规划共用任务编辑时均等待数据库确认后更新缓存、关闭弹窗；失败保留草稿并允许重试，任务表单继续复用现有校验。初始焦点统一交由模态组件管理，避免输入框提前自动聚焦导致关闭后丢失入口。安排、改期、退回使用原子 `transition_task`。

迁移 `202609060001_task_planning.sql` 新增四参数 RPC：`p_task_id`、`p_transition`、`p_target_date`、`p_time_zone`。客户端传运行设备的 IANA 时区；数据库据此校验目标不早于今天。改期允许提前或推后，拒绝原日期及已完成任务；历史保留 from/to 日期，实际投入仍在原账本日期。三参数入口保持可调用，按 UTC 校验。

此迁移只替换函数、设置新重载权限，不执行历史数据更新或清理。应先部署迁移再发布新客户端；部署状态与本地验证分开报告。

本期不提供 Daily 未来展开、多日并列、拖拽排程或外部日历同步。原 analytics 仍供洞察和报告使用。时间轴仅改变同一 React 客户端视图与派生布局，Web/PWA 和 Electron 共用；没有修改数据库、数据仓储或 Next/Electron 运行边界。

## 验证

单元测试覆盖分组、密度、未知估时、周跨年及改期日期；浏览器测试覆盖新建、提前改期、完成、待安排转入和首页日期隔离，并输出截图和 axe 报告。数据库测试在事务内验证历史、账本与 RLS，结束回滚。

Playwright iPhone WebKit 属于浏览器自动化，不能替代真实 iPhone Safari/PWA；打包 Windows 应用和真实设备需分别验收。

### 单日时间轴改版验证（2026-09-07）

- 时间布局、原规划规则与 CSS token 合同共 3 个文件、12 项单元测试通过；覆盖独立估时、未知结束、短任务相邻碰撞、密集分栏和午夜边界。
- 规划浏览器回归 24/24 通过：桌面 Chromium、手机 Chromium、iPhone WebKit 各 8 项，包含真实比例、完整详情、编辑/完成/改期/退回待安排、刷新持久化、入口消失后的焦点回退和滚动吸顶位置。
- 浅色、深色、320px 窄屏、430px 手机、844×390 横屏及放大根字号通过水平边界与 axe serious/critical 检查；时间块点击区域均至少 44×44 CSS px。实际截图已逐张检查并更新至 [截图说明](screenshots/task-planning/README.md)。
- 显式本地测试适配器的 Web production 构建、TypeScript、改动范围 ESLint 和格式检查通过。没有执行生产数据库写入、线上部署、真实 iPhone Safari/PWA 或打包 Electron 实机验收。

### 初版业务验证记录（cfb71f5）

- 全量单元测试：48 个文件、211 项通过；类型检查、修改范围 ESLint 和静态 SQL 合同通过。
- Web 生产构建通过；桌面 Chromium、手机 Chromium、iPhone WebKit 验证规划流程，结构与 axe 无障碍回归通过，月历与日详情的密集/稀疏演示另有截图及焦点恢复验证。
- 本地 PostgreSQL：10 项规划 pgTAP 通过；测试库缺少的前置迁移与本次迁移均在同一事务中加载，结束回滚。
- 尚未执行：生产数据库迁移部署、真实 iPhone Safari/PWA、打包 Windows 应用实机验收。

### 月历首页视觉修订

本次全量单元测试 48 个文件、212 项通过；规划流程、结构与无障碍浏览器回归 24/24 通过，最终跨月热力和旧导航专项回归 5/5 通过。Web 生产构建、TypeScript、修改范围 ESLint 与格式检查通过。

端到端覆盖整月密集安排（含 12 项）、空日期、单项详情、深浅外观、返回来源月份与入口焦点、320px 窄屏、844×390 横屏和放大根字号。深色检查等待实际 CSS 外观过渡完成后读取稳定颜色，没有豁免小字对比度规则。视觉材料见 [截图说明](screenshots/task-planning/README.md)。本次没有部署生产迁移或执行真实设备验收。

### 合并前保存与焦点回归

首页任务编辑接入确认保存路径，取消输入框抢先自动聚焦，规划新建入口显式记录焦点。焦点恢复用例先复现失败，修复后表单与创建编辑规则共 11 项通过；真实本地 Supabase 浏览器回归覆盖首页编辑收到 503 时保留草稿、成功重试后刷新持久化，并保留慢网勾选、双页面同步与退出登录检查。测试前向补齐了本地测试库迁移，未重置数据库。Web 生产构建与修改范围 ESLint 通过。

### 生产迁移与最终检查

用户确认后于 2026-09-07 执行生产迁移 `202609060001`，CLI 仅应用此函数迁移；执行后复查全部本地/生产迁移版本一致。未运行生产任务操作或改写历史数据。

全量 CI 的节律页深色测试曾读到外观切换中的背景色；该测试现在与规划测试一致，等待浏览器实际动画结束后检查稳定配色，保留全部 axe 断言。iPhone WebKit 的首页、洞察、设置、节律四项字体与深浅外观回归通过；Web 生产构建、格式及 ESLint 通过。此项只修改测试等待时机，README 产品行为无需变更。
